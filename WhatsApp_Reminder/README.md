# 💬 Module 2: Autonomous Client Reminder Dispatcher

This infrastructure module automates accounts receivable collections by scanning outstanding ledger files and routing personalized, context-aware payment reminders directly to client accounts using automated browser controls.

---

## 🛠️ Prerequisites & Installation

This automation module utilizes browser-routing layers. Ensure you have the dependencies installed:

```bash
pip install pandas openpyxl pywhatkit